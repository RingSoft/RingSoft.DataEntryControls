<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dec="clr-namespace:RingSoft.DataEntryControls.WPF;assembly=RingSoft.DataEntryControls.WPF"
                    xmlns:local="clr-namespace:RingSoft.DataEntryControls.WPFToolKitCore">
    <Style TargetType="{x:Type dec:DecimalEditControl}" x:Key="XceedDecimalEditControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type dec:DecimalEditControl}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Name="TextBox" Height="20"/>
                                <Button Name="DropDownButton" Grid.Row="0" Grid.Column="1" Width="16" VerticalAlignment="Stretch">
                                    <Image
                                        Source="/RingSoft.DataEntryControls.WPF;component/Resources/calculator16.png">
                                    </Image>
                                </Button>
                            </Grid>
                            <Popup x:Name="Popup" PlacementTarget="{Binding ElementName=TextBox}" StaysOpen="True" Placement="Bottom" AllowsTransparency="True">
                                <Grid>
                                    <local:XceedCalculatorControl x:Name="Calculator"/>
                                    <!--<local:DummyCalculator x:Name="Calculator"/>-->
                                </Grid>
                            </Popup>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>